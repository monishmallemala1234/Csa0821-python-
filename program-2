Given an integer x, return true if x is a palindrome and false otherwise.


a=input()

a=a.lower()

b=""

b=b.lower()

for v in a:

    b=v+b

if b==a:

    print("Palindrome")
else:

    print("Not a Palindrome")
