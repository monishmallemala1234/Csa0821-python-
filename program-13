You are given two strings s and t. String t is generated by random shuffling string s and then add one
more letter at a random position. Return the letter that was added to t.



def find_the_difference(s: str, t: str) -> str:
    char_count = [0] * 26  # Initialize count array for lowercase English letters

  
    for char in s:
        char_count[ord(char) - ord('a')] += 1

    for char in t:
        char_count[ord(char) - ord('a')] -= 1
        if char_count[ord(char) - ord('a')] < 0:
            return char

# Test cases
print(find_the_difference("abcd", "abcde"))  
print(find_the_difference("", "y"))           
